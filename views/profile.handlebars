{{!-- {{#if posts.length}}
<div class="container">
  <h2>Your Posts</h2>
    {{#each posts as |post|}}
      {{> dashboard-postinfo post}}
    {{/each}}
    
</div>
{{/if}} --}}
<div class="container">
  {{#each posts as |post|}}
  <div class="container-md border border-3 d-flex justify-content-between mb-2">
    <h5><a href="/edit/{{post.id}}">{{post.title}}</a></h5>
    <p>{{format_date post.date_created}}</p>
  </div>
  {{/each}}

  <div class="row mt-4">
    <a class="link btn btn-primary" href="/new-post">+ New Post</a>
  </div>
</div>
  



{{!-- <button type="submit" class="btn"><a class="link" href="/profile/new-post">+ New Post!</a></button> --}}

{{!-- <script src="/public/js/add-post.js"></script> --}}

